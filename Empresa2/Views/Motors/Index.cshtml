@model IEnumerable<Empresa2.Models.Motor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Marca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoDeMotor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Marca)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoDeMotor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMotor">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMotor">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMotor">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
